#include <stdio.h>
#include <stdlib.h>
#include <time.h>

int main() {
    int n, i;
    srand((unsigned)time(NULL));
    n = rand() % 10 + 1; // 随机生成1到10之间的数字
    printf("随机生成的数字个数: %d\n", n);

    return 0;
}